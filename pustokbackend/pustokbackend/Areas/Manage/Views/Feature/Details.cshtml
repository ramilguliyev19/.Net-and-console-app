@model Feature

<div class="container">
    <div class="row">
        <div class="col-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <h4 class="card-title">Basic Table</h4>
                        <button class="btn btn-success">Create</button>
                    </div>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                            <tr>
                                <th>Id</th>
                                <th>Icon-Url</th>
                                <th>Title1</th>
                                <th>Title2</th>
                            </tr>
                            </thead>
                            <tbody>
                            <tr>
                                    <td>@Model.Id</td>
                                    <td>
                                        <i class="fas @Model.IconUrl"></i>
                                    </td>
                                    <td>@Model.Title1</td>
                                    <td>
                                        @Model.Title2
                                    </td>
                                    <td>
                                        <a class="btn btn-warning" asp-area="manage" asp-controller="Feature" asp-action="Update" asp-route-id="@Model.Id">Update</a>
                                        <form method="post">
                                            <button class="btn btn-danger" asp-area="manage" asp-controller="Feature" asp-action="Delete" asp-route-id="@Model.Id">Delete</button>
                                        </form>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>