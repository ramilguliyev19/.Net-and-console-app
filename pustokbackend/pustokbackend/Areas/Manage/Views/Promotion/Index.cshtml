@model List<Promotion>

    <div class="container">
        <div class="row">
            <div class="col-lg-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                    <div class="d-flex justify-content-between">
                        <button type="button" class="btn btn-primary mb-5">
                            <a class="text-decoration-none text-white" asp-area="manage"
                               asp-action="index" asp-controller="dashboard">
                                Go To Back
                            </a>
                        </button>
                        <button type="button" class="btn btn-success mb-5">
                            <a class="text-decoration-none text-white" asp-area="manage"
                               asp-action="create" asp-controller="promotion">
                                Create
                            </a>
                        </button>
                    </div>

                  <h1 class="card-title">First Promotions</h1>
                  
                  <div class="table-responsive">
                    <table class="table table-striped">
                      <thead>
                        <tr class="text-center">
                          <th>
                            Redirect Url
                          </th>
                          <th>
                             Redirect Text
                          </th>
                          
                        </tr>
                      </thead>
                      <tbody class="text-center">
                         @foreach (var promotion in @Model)
                        {
                            <tr>
                              <td>
                                  @promotion.RedirectUrl
                              </td>
                              <td>
                               <div class="hero-partial-image image-left">
                                    <img src="~/assets/image/bg-images/@promotion.RedirectText" alt="">
                               </div>
                              </td>
                              <td>

                              <a asp-area="manage" asp-controller="promotion" 
                                asp-action="details" asp-route-id="@promotion.Id" 
                                class="btn btn-primary my-1">Details
                                </a>

                                <a asp-area="manage" asp-controller="promotion" 
                                asp-action="update" asp-route-id="@promotion.Id"  
                                class="btn btn-warning my-1">Update
                                </a>
                                
                                <a asp-area="manage" asp-controller="promotion" 
                                asp-action="delete" asp-route-id="@promotion.Id"  
                                class="btn btn-danger my-1">Delete
                                </a>   
                              </td>
                          </tr>
                        }
                        
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </div>
        </div>
    </div>